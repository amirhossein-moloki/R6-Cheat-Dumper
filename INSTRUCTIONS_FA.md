# راهنمای جامع ساخت (Build) و اجرای پروژه R6-Dumper-External

این راهنما شامل مراحل کامل برای کامپایل و اجرای سه بخش اصلی پروژه (درایور، دامپر و چیت) برای ویندوز ۶۴ بیتی است.

## ۱. پیش‌نیازهای نرم‌افزاری
برای ساخت این پروژه، شما به موارد زیر در محیط ویندوز نیاز دارید:
- **Visual Studio 2019 یا 2022**: هنگام نصب، گزینه "Desktop development with C++" را انتخاب کنید.
- **Windows SDK**: آخرین نسخه SDK ویندوز.
- **Windows Driver Kit (WDK)**: برای ساخت بخش درایور (`memdrv`) الزامی است.
- **NuGet Package Manager**: برای بازگردانی وابستگی‌های پروژه.

---

## ۲. کامپایل اجزای پروژه

### الف) ساخت درایور (memdrv)
درایور وظیفه دسترسی به حافظه بازی بدون شناسایی توسط آنتی‌چیت را دارد.
1. به پوشه `memdrv/` بروید.
2. فایل `memdrv.sln` را با Visual Studio باز کنید.
3. تنظیمات بیلد را روی **Release** و پلتفرم را روی **x64** قرار دهید.
4. پروژه را بیلد کنید (`Ctrl + Shift + B`). خروجی یک فایل با پسوند `.sys` خواهد بود.
   - **روش جایگزین (خط فرمان)**:
     ```bash
     msbuild memdrv.sln /p:Configuration=Release /p:Platform=x64 /p:SpectreMitigation=false /t:Rebuild /v:minimal
     ```
5. **نکته مهم**: درایور یک دیوایس با نام `\Device\MemDrv` ایجاد می‌کند. درایورهای ویندوز باید دارای امضای دیجیتال (Signature) باشند. برای تست، باید ویندوز را در حالت **Test Mode** قرار دهید یا از درایور مپرهایی مثل `kdmapper` استفاده کنید.

### ب) ساخت دامپر (r6dumper)
دامپر برای استخراج آفست‌های جدید بازی بعد از هر آپدیت استفاده می‌شود.
1. به پوشه `r6dumper/` بروید.
2. فایل `r6od.sln` را باز کنید.
3. تنظیمات را روی **Release** و **x64** قرار دهید.
4. پروژه را بیلد کنید. خروجی یک فایل `.exe` است.
5. فایل `offsets.db` موجود در پوشه `src` باید در کنار فایل اجرایی باشد.

### ج) ساخت چیت (r6external)
این برنامه اصلی چیت است که دارای محیط گرافیکی (Overlay) می‌باشد.
1. به پوشه `r6external/` بروید.
2. فایل `rainbowsix-external.sln` را باز کنید.
3. تنظیمات را روی **Release** و **x64** قرار دهید.
4. وابستگی‌های NuGet را بازگردانی (Restore) کنید.
5. پروژه را بیلد کنید.
6. **توجه**: این پروژه به صورت یک **DLL** خروجی می‌دهد. برای اجرا، باید آن را با یک اینجکتور به یک پروسه تزریق کنید، یا تنظیمات پروژه را به `Application (.exe)` تغییر دهید.
7. **نکته**: پروژه از نسخه‌های محلی ImGui و MinHook (واقع در پوشه `overlay`) استفاده می‌کند.

---

## ۳. مراحل اجرا
برای استفاده صحیح، مراحل را به این ترتیب دنبال کنید:

1. **بارگذاری درایور**: ابتدا درایور `memdrv.sys` را بارگذاری کنید. (بدون درایور، چیت و دامپر با ارور "Driver not loaded" مواجه می‌شوند).
2. **اجرای بازی**: بازی Rainbow Six Siege را اجرا کنید و به منوی اصلی بروید.
3. **بروزرسانی آفست‌ها (اختیاری)**: اگر بازی آپدیت شده است، `r6dumper.exe` را اجرا کنید تا آفست‌های جدید را پیدا کند.
4. **اجرای چیت**: فایل چیت را اجرا (یا تزریق) کنید.
   - کلید **INSERT**: باز و بسته کردن منوی چیت.
   - کلید **DELETE**: بستن کامل برنامه چیت.

## ۴. عیب‌یابی (Troubleshooting)
- **ارور "Driver not loaded"**: مطمئن شوید درایور به درستی لود شده و ویندوز در حالت تست است.
- **ارور DirectX**: برای نمایش منوی چیت، باید DirectX 11 روی سیستم شما نصب باشد.
- **بسته شدن ناگهانی**: آفست‌های موجود در فایل `offsets.hpp` را با آفست‌های جدیدی که دامپر استخراج می‌کند، مطابقت دهید.
