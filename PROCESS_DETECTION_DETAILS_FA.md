# جزئیات فنی مکانیزم شناسایی پروسه بازی

این سند به توضیح دقیق نحوه پیدا کردن و شناسایی پروسه بازی Rainbow Six Siege در این پروژه می‌پردازد.

## روش‌های شناسایی (Identification Methods)

پروژه از سه لایه امنیتی و کاربردی برای پیدا کردن بازی استفاده می‌کند:

### ۱. اسکن لیست پروسس‌ها (Process Snapshot)
این روش اصلی و قابل اعتمادترین راه برای پیدا کردن بازی است.
*   **نحوه عملکرد:** با استفاده از توابع Windows API (`CreateToolhelp32Snapshot` و `Process32Next`)، یک کپی از لیست تمام برنامه‌های در حال اجرا تهیه می‌شود.
*   **فایل‌های مورد جستجو:**
    *   `RainbowSix.exe` (نسخه استاندارد API DirectX)
    *   `RainbowSix_Vulkan.exe` (نسخه مخصوص API Vulkan)
*   **مزیت:** این روش وابسته به باز بودن پنجره نیست و حتی اگر بازی در حال لود شدن باشد، آن را پیدا می‌کند.

### ۲. جستجوی عنوان پنجره (Window Title)
اگر اسکن فایل‌ها ناموفق باشد، برنامه به دنبال پنجره فعال بازی می‌گردد.
*   **نحوه عملکرد:** استفاده از تابع `FindWindowA` با پارامتر عنوان پنجره.
*   **عنوان مورد جستجو:** `"Rainbow Six"`
*   **تکمیل فرآیند:** پس از یافتن پنجره، با استفاده از `GetWindowThreadProcessId` نشانگر عددی پروسه (PID) استخراج می‌شود.

### ۳. کلاس اختصاصی پنجره (Window Class)
این روش بسیار دقیق است زیرا کلاس پنجره برخلاف عنوان آن، معمولاً توسط کاربر یا ابزارهای دیگر تغییر نمی‌کند.
*   **کلاس مورد جستجو:** `"R6Game"`
*   **نحوه عملکرد:** مشابه روش عنوان پنجره، اما با تمرکز بر Class Name ثبت شده در سیستم‌عامل.

## منطق اولویت‌بندی (Priority Logic)

در کد `util.cpp` تابع `is_game_open` به صورت زیر عمل می‌کند:
1. ابتدا هر سه مقدار (Window، Class، File) را چک می‌کند.
2. اگر PID از طریق **فایل (EXE)** پیدا شود، آن را به عنوان اولویت اول و قطعی در نظر می‌گیرد.
3. در غیر این صورت، اگر از طریق نام پنجره یا کلاس پیدا شود، اجازه ادامه عملیات را می‌دهد.

## چرا از چند روش استفاده می‌شود؟
*   **پایداری (Stability):** در صورت تغییر نام فایل اجرایی توسط یوبی‌سافت در آپدیت‌های جدید، روش‌های پنجره‌ای همچنان کار می‌کنند.
*   **پشتیبانی از Vulkan:** از آنجایی که بازی دو نسخه اجرایی متفاوت دارد، این سیستم هر دو را پوشش می‌دهد.
*   **دور زدن آنتی‌چیت:** BattlEye ممکن است برخی دسترسی‌ها را محدود کند؛ داشتن روش‌های جایگزین (Fallback) ریسک عدم شناسایی را به صفر می‌رساند.

---
*این فایل جهت راهنمایی توسعه‌دهندگان در مورد بخش `util` و `main` پروژه تهیه شده است.*
