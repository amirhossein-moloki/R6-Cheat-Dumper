# تحلیل فنی پروژه R6-Dumper-External

این سند شامل تحلیل فنی نحوه عملکرد بخش‌های مختلف این پروژه است.

## ۱. نحوه کار Dumper (استخراج Offsetها)
دامپر وظیفه دارد مکان دقیق متغیرها و توابع بازی را در حافظه (RAM) پیدا کند. چون با هر آپدیت بازی، این آدرس‌ها تغییر می‌کنند، دامپر از دو روش اصلی استفاده می‌کند:

### Signature Scanning (جستجوی الگو)
دامپر به دنبال "امضای" خاصی از بایت‌ها می‌گردد. مثلاً اگر یک تابع همیشه با بایت‌های `48 8B 05` شروع شود، دامپر کل فایل بازی را برای پیدا کردن این الگوی خاص جستجو می‌کند. این کار در فایل `shared.h` با تابع `sig_scan` و به صورت چند‌رشته‌ای (Multi-threaded) برای سرعت بیشتر انجام می‌شود.

### String XRefs (ارجاع از طریق متن)
بسیاری از آفست‌ها با پیدا کردن یک متن خاص (String) در فایل بازی پیدا می‌شوند. دامپر ابتدا آدرس یک متن (مثلاً "PlayerDamage") را در بخش `.rdata` پیدا می‌کند، سپس در بخش کدهای بازی (`.text`) جستجو می‌کند تا ببیند کدام دستورالعمل به آن آدرس اشاره کرده است. با پیدا کردن آن دستور، آفست مربوطه استخراج می‌شود.

---

## ۲. نحوه کار Cheat (چیت خارجی)
این چیت از نوع **External** است، یعنی به جای اینکه داخل بازی تزریق (Inject) شود، به عنوان یک برنامه جداگانه اجرا می‌شود.

### استفاده از Driver
برای اینکه آنتی‌چیت (BattleEye) متوجه نشود، چیت مستقیماً از توابع ویندوز برای خواندن حافظه استفاده نمی‌کند. در عوض از یک درایور سطح کرنل (`memdrv`) استفاده می‌کند تا به حافظه بازی دسترسی پیدا کند. این درایور اجازه می‌دهد چیت بدون دیده شدن توسط آنتی‌چیت، مقادیر حافظه را بخواند یا تغییر دهد.

### Glow ESP (دیدن از پشت دیوار)
چیت آدرس `glow_manager` را پیدا می‌کند. سپس مقادیر رنگ (Red, Green, Blue) و شفافیت (Alpha) را در آدرس‌های مشخصی می‌نویسد تا بازیکنان در بازی هایلایت شوند (فایل `glow.cpp`).

### No Recoil (حذف لگد اسلحه)
چیت یک "زنجیره پوینتر" (Pointer Chain) را دنبال می‌کند تا به شیء مربوط به اسلحه برسد. سپس مقادیری مثل `vertical_recoil` و `horizontal_recoil` را به صفر یا اعداد کوچک تغییر می‌دهد (فایل `norecoil.cpp`).

### حلقه اصلی (Main Loop)
برنامه مدام در یک حلقه چک می‌کند که آیا بازی باز است یا خیر، آدرس‌ها را آپدیت می‌کند و ویژگی‌هایی مثل Glow و NoRecoil را در هر لحظه اعمال می‌کند.

---

## ۳. اجزای پروژه
- **memdrv:** درایور سطح کرنل برای دسترسی امن به حافظه.
- **r6dumper:** ابزاری برای استخراج آفست‌های جدید بعد از هر آپدیت بازی.
- **r6external:** برنامه اصلی چیت که از آفست‌ها و درایور استفاده می‌کند.

---
*نکته: طبق فایل README، این کدها لو رفته (Leaked) هستند و صرفاً جهت آموزش قرار داده شده‌اند.*
