# راهنمای فنی: درایور، معماری و مکانیزم شناسایی پروسس

این سند برای پاسخ به سوالات فنی در مورد چرایی نیاز به درایور، نوع معماری پروژه و بهبودهای اعمال شده در سیستم تشخیص بازی تهیه شده است.

## ۱. چرا به درایور (Kernel-mode Driver) نیاز داریم؟
بازی Rainbow Six Siege از آنتی‌چیت **BattlEye** استفاده می‌کند که در سطح هسته سیستم‌عامل (Kernel Level / Ring 0) فعالیت می‌کند.

*   **محدودیت‌های سطح کاربر (User-mode):** در حالت عادی، ویندوز اجازه نمی‌دهد یک برنامه معمولی به حافظه برنامه دیگری دسترسی کامل داشته باشد، مخصوصاً اگر آن برنامه توسط آنتی‌چیت محافظت شود. آنتی‌چیت با استفاده از Callbacks، دسترسی توابعی مثل `OpenProcess` را مسدود یا محدود می‌کند.
*   **دور زدن آنتی‌چیت (Anti-Cheat Bypass):** درایور این پروژه (`memdrv`) در همان سطح آنتی‌چیت اجرا می‌شود. این یعنی می‌تواند محدودیت‌های اعمال شده را نادیده بگیرد و مستقیماً به حافظه بازی دسترسی پیدا کند، بدون اینکه BattlEye متوجه حضور آن شود.
*   **امنیت و پنهان‌کاری:** خواندن حافظه از طریق درایور بسیار امن‌تر از روش‌های سنتی ویندوز است و احتمال شناسایی (Detection) را به حداقل می‌رساند.

## ۲. چیت اکسترنال (External) یا اینترنال (Internal)؟
این پروژه به صورت **اکسترنال (External)** طراحی شده است.
*   **تفاوت:** چیت‌های اینترنال کد خود را به داخل پروسس بازی تزریق (Inject) می‌کنند که ریسک شناسایی بالایی دارد. اما این چیت به عنوان یک برنامه کاملاً مجزا اجرا شده و فقط حافظه بازی را "از بیرون" می‌خواند و تغییر می‌دهد.
*   **رابط گرافیکی (Overlay):** این چیت از تکنیک **DWM Hooking** استفاده می‌کند. یعنی منوها و قابلیت‌های بصری را مستقیماً روی Desktop Window Manager ویندوز رسم می‌کند، نه روی خود بازی. این کار باعث می‌شود آنتی‌چیت نتواند با اسکرین‌شات گرفتن از بازی، چیت را پیدا کند.

## ۳. آیا بدون درایور امکان استفاده هست؟
بله، در کدها قابلیتی به نام **User-mode Fallback** قرار داده شده است.
*   اگر درایور لود نشود، برنامه از شما می‌پرسد که آیا می‌خواهید بدون آن ادامه دهید.
*   **هشدار:** استفاده بدون درایور در بازی‌های آنلاین منجر به **بن شدن فوری** می‌شود. این حالت فقط برای محیط‌های آموزشی یا بازی‌های بدون آنتی‌چیت کاربرد دارد.

## ۴. بهبود سیستم تشخیص پروسس (Professional Process Detection)
در نسخه جدید، مکانیزم پیدا کردن بازی از حالت "ساده" به حالت "حرفه‌ای و چندلایه" تغییر یافته است:
*   **اولویت با اسکن لیست پروسس‌ها:** برنامه ابتدا تمام پروسس‌های در حال اجرا را اسکن می‌کند تا فایل‌های اجرایی اصلی (`RainbowSix.exe` یا `RainbowSix_Vulkan.exe`) را پیدا کند.
*   **پشتیبانی از ولکان (Vulkan):** بسیاری از بازیکنان از نسخه Vulkan استفاده می‌کنند که در نسخه‌های قبلی شناسایی نمی‌شد، اما اکنون به طور کامل پشتیبانی می‌شود.
*   **سیستم جایگزین (Fallback):** اگر اسکن لیست پروسس‌ها به دلیلی ناموفق باشد، برنامه به عنوان لایه دوم از جستجوی پنجره (Window Title) استفاده می‌کند تا پایداری ۱۰۰ درصدی داشته باشد.

---
*این مستند جهت شفاف‌سازی فنی برای توسعه‌دهندگان و کاربران حرفه‌ای تهیه شده است.*
