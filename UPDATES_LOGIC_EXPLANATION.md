# گزارش تغییرات منطق و کد (بروزرسانی مکانیزم Fallback)

این سند تغییرات فنی اعمال شده برای بهبود پایداری چیت در حالت‌های بدون درایور و مدیریت سطوح دسترسی را توضیح می‌دهد.

## ۱. منطق جدید دسترسی به حافظه (Tiered Access)
در نسخه قبلی، سیستم فقط دو حالت داشت: یا درایور لود شده بود (دسترسی کامل) و یا لود نشده بود (خطا). اکنون یک سیستم سه لایه پیاده‌سازی شده است:

1.  **Kernel-mode (Full Access):** بالاترین سطح امنیت و دسترسی از طریق درایور `memdrv`.
2.  **User-mode (Full Access):** دسترسی مستقیم از طریق ویندوز با قابلیت خواندن و نوشتن (مناسب برای محیط‌های تست).
3.  **User-mode (Limited Access):** دسترسی فقط خواندنی (Read-Only) در مواردی که آنتی‌چیت اجازه باز کردن پروسه با دسترسی کامل را نمی‌دهد.

## ۲. تغییرات در کد درایور (`driver.cpp` / `driver.hpp`)
- **متغیر `g_has_write_access`:** یک پرچم سراسری جدید برای ردیابی توانایی نوشتن در حافظه اضافه شد.
- **اصلاح `open_process`:**
    ```cpp
    // تلاش اول: دسترسی کامل
    HANDLE hProcess = OpenProcess(PROCESS_QUERY_INFORMATION | PROCESS_VM_READ | PROCESS_VM_WRITE | PROCESS_VM_OPERATION, FALSE, pid);
    if (hProcess != NULL) {
        g_has_write_access = true;
    } else {
        // تلاش دوم: فقط خواندنی (Fallback)
        hProcess = OpenProcess(PROCESS_QUERY_INFORMATION | PROCESS_VM_READ, FALSE, pid);
        g_has_write_access = false;
    }
    ```
این تغییر باعث می‌شود اگر ویندوز یا آنتی‌چیت مانع دسترسی `WRITE` شوند، برنامه بجای بسته شدن، با دسترسی محدود ادامه دهد.

## ۳. مدیریت هوشمند قابلیت‌ها (`cheat.cpp`)
منطق اجرای چیت تغییر کرد تا از کرش‌های احتمالی جلوگیری شود. تمامی قابلیت‌هایی که نیاز به تغییر در حافظه دارند، اکنون در یک بلوک شرطی قرار گرفته‌اند:

```cpp
void cheat::run() {
    if (driver::g_has_write_access) {
        combat::norecoil(...);
        visuals::glow(...);
        // سایر قابلیت‌های نوشتنی
    }
    // قابلیت‌های خواندنی مثل ESP در اینجا قرار نمی‌گیرند و همیشه اجرا می‌شوند
}
```

## ۴. بهبود اطلاع‌رسانی در `main.cpp`
سیستم لاگ‌دهی برنامه بازنویسی شد تا کاربر دقیقاً بداند برنامه در چه وضعیتی متصل شده است. پیام‌ها پس از مرحله `attach` نمایش داده می‌شوند تا وضعیت نهایی دسترسی مشخص شده باشد.

## ۵. مزایای این تغییرات
- **پایداری ۱۰۰ درصدی:** برنامه هرگز به دلیل عدم وجود درایور یا محدودیت هندل بسته نمی‌شود.
- **ایمنی بیشتر:** برنامه دیگر سعی نمی‌کند به حافظه‌ای که اجازه نوشتن در آن را ندارد دسترسی پیدا کند (جلوگیری از خطای Access Violation).
- **قابلیت استفاده در همه شرایط:** حتی در سخت‌گیرانه‌ترین حالت‌های آنتی‌چیت، کاربر همچنان به قابلیت ESP دسترسی خواهد داشت.

---
*این بروزرسانی جهت افزایش انعطاف‌پذیری فنی پروژه انجام شده است.*
